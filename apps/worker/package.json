{
  "name": "@fintiex/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "echo 'no build needed; runs via tsx'",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint'"
  },
  "dependencies": {
    "@fintiex/db": "workspace:*",
    "node-cron": "3.0.3",
    "tsx": "4.19.2"
  },
  "devDependencies": {
    "@types/node-cron": "3.0.11",
    "vitest": "2.1.4"
  }
}
