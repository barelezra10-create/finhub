import {conversionReport} from '@/lib/analytics/conversions';
export async function ConversionPanel({days,status}:{days:number;status?:string}){
 let rows:Awaited<ReturnType<typeof conversionReport>>|null=null;
 try{rows=await conversionReport(days);}catch{}
 return <><section className="analytics-notice"><strong>Partner-reported applications and commissions</strong><p>Import actual partner reports. Traffic source, country and device filters do not apply here: a partner connection with a shared click ID is required for that attribution. Date selection uses the conversion date reported by the partner. Submitted, approved and paid are separate stages; currencies are never combined.</p></section><section className="analytics-panel"><h2>Applications &amp; revenue</h2>{rows===null?<p>Conversion storage is temporarily unavailable.</p>:!rows.length?<p className="analytics-empty">No partner conversions imported for this period. Revenue is unknown until partner reporting is connected or imported.</p>:<div className="analytics-scroll"><table><thead><tr>{['Network','Offer','Currency','Reported applications','Approved or paid','Paid','Rejected','Approved commission (unpaid)','Paid commission'].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={[r.network,r.offer,r.currency].join(':')}>{[r.network,r.offer,r.currency,r.applications,r.approved,r.paid,r.rejected,r.approved_commission,r.paid_commission].map((v,i)=><td key={i}>{String(v)}</td>)}</tr>)}</tbody></table></div>}</section><section className="analytics-panel"><h2>Import a partner report</h2><p>Use one row per conversion, with the partner’s opaque conversion ID. Reimporting the same network and ID updates its status and commission without double counting. Revenue means commission payable to Fintiex, not the customer’s loan amount or spending. Do not include names, emails, account numbers or other customer details.</p>{status&&<p role="status">{status==='success'?'Report imported successfully.':'Import failed. Check the CSV format, dates, amounts and duplicate IDs; no rows were changed.'}</p>}<p className="small analytics-muted">CSV header (plain comma-separated values; no quoted fields):</p><pre className="analytics-scroll">network,conversion_id,occurred_at,offer,status,revenue,currency</pre><p className="small analytics-muted">Date: YYYY-MM-DD. Status: submitted, approved, paid, or rejected. Commission: nonnegative number with up to two decimal places; rejected rows must be 0. Currency: three uppercase letters. IDs: letters, digits, underscores, hyphens, dots or colons. Maximum 5,000 rows / 1 MB.</p><form action="/api/admin/conversions/import" method="post" encType="multipart/form-data" className="analytics-filters"><label>Partner report<input required type="file" name="file" accept=".csv,text/csv"/></label><button>Import report</button></form><p className="small analytics-muted">Automatic partner synchronization is not configured. Imported conversion records are retained for reporting; raw CSV files are not saved.</p></section></>;
}
