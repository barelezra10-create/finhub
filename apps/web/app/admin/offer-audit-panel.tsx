import Link from 'next/link';
import {loadCards} from '@/lib/cards-server';
export function OfferAuditPanel(){const cards=loadCards();return <section className="analytics-panel"><h2>Card source audit</h2><p>{cards.length} records reviewed for source availability. A selected-facts check does not certify all pricing. Missing rates are withheld from the directory. The original data remains in version history.</p><div className="analytics-scroll"><table><thead><tr><th>Card</th><th>Status</th><th>Source check</th><th>Confirmed fields</th><th>Official source</th></tr></thead><tbody>{cards.map(c=><tr key={c.slug}><td><Link href={`/credit-cards/${c.slug}`}>{c.issuer} {c.name}</Link></td><td>{c.availability}</td><td>{c.source_checked||'Pending'}</td><td>{c.verified_fields?.join(', ')||'None confirmed'}</td><td><a href={c.source_url||c.application_url} target="_blank" rel="noopener noreferrer">Open source</a></td></tr>)}</tbody></table></div></section>}
