import {notFound} from 'next/navigation';
import {SavingsReview,savingsReviewMetadata} from '@/components/savings-review';
const accounts:Record<string,string>={'cit-savings-connect':'cit-savings-connect','synchrony-high-yield-savings':'synchrony','lendingclub-high-yield-savings':'lendingclub-savings','marcus-online-savings':'marcus','ally-online-savings':'ally','american-express-personal-savings':'amex-savings','capital-one-360-performance-savings':'capital-one','discover-online-savings':'discover-savings'};
export function generateStaticParams(){return Object.keys(accounts).filter(slug => !["marcus-online-savings", "ally-online-savings", "american-express-personal-savings", "discover-online-savings"].includes(slug)).map(slug=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!accounts[slug])return {};const m=savingsReviewMetadata(accounts[slug],`/savings/accounts/${slug}`);return {...m,title:m.title.replace('Review:','Account Details:')};}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!accounts[slug])notFound();return <SavingsReview offerKey={accounts[slug]} path={`/savings/accounts/${slug}`}/>;}
